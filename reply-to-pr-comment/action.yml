name: 'Bloat Comment'
description: 'Post binary size differences as a comment on a pr'
inputs:
    stats:
        description: 'stats object generated by bloatcmp'
        required: true
    myToken:
        description: 'GitHub API token'
        required: true
runs:
  using: 'node12'
  main: 'index.js'
