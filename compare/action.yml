name: 'Bloat compare size'
description: 'Compare two sets of file sizes'
inputs:
    old:
        description: 'a sizes object in the format generated by ./get-sizes'
        required: true
    new:
        description: 'a sizes object in the format generated by ./get-sizes'
        required: true
outputs:
  stats: # id of output
    description: 'json describing size changes for each item in the input'
runs:
  using: 'node12'
  main: 'index.js'
