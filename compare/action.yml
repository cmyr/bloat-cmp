name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
    old:
        description: 'a sizes object in the format generated by ./get-sizes'
        required: true
    new:
        description: 'a sizes object in the format generated by ./get-sizes'
        required: true
outputs:
  bloat_stats: # id of output
    description: 'json describing size changes for each item in the input'
runs:
  using: 'node12'
  main: 'index.js'
